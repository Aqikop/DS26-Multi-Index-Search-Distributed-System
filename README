PORTNUM:
    COORDINATOR: 8080
    RECIPE-NODE: 8100
    LLM-NODE: 8120

    CACHE-NODE: 8140
    USER-NODE: 8160

    ETL-NODE: 8180

INSTRUCTION:
    (One-time only)
        python -m venv .venv
        .\.venv\Scripts\Activate.ps1
        pip install -r requirements.txt

    .\.venv\Scripts\Activate.ps1
    python -m uvicorn app:app --host 127.0.0.1 --port 5000 

    mvn spring-boot:run

    Invoke-RestMethod -Method Post -Uri "http://localhost:8080/apply?id=localhost:8120&type=llm"
    Invoke-RestMethod -Method Post -Uri "http://localhost:8080/apply?id=localhost:8100&type=db"